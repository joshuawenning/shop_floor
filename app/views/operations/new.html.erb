<% content_for :title, "New Operation | Shop Floor" %>

<div class="breadcrumb">
  <%= link_to "Work Orders", work_orders_path %>
  <span>/</span>
  <%= link_to @work_order.number, @work_order %>
  <span>/</span>
  <span>New operation</span>
</div>

<header class="page-heading page-heading--form">
  <div>
    <p class="eyebrow">Routing sequence / initiate</p>
    <h1>New Operation</h1>
    <p class="page-heading__description">Work order <%= @work_order.number %> / append manufacturing step</p>
  </div>
</header>

<%= render "form", work_order: @work_order, operation: @operation %>
