<% flash.each do |type, message| %>
  <div class="flash flash--<%= type %>" role="status">
    <span class="flash__label"><%= type.to_s.upcase %></span>
    <span><%= message %></span>
  </div>
<% end %>
